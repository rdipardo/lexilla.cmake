language: cpp
os: linux
dist: focal
compiler:
  - clang
  - gcc
script:
  - cd lexilla/scripts
  - ./RunTest.sh
jobs:
  include:
    - os: osx
      env:
        - CLANG=1
      osx_image: xcode12
      script:
        - cd lexilla/scripts
        - ./RunTest.sh
    - os: windows
      install:
        - choco install -y visualstudio2019buildtools --package-parameters "--add Microsoft.VisualStudio.Component.VC.Tools.x86.x64"
      script:
        - export CXX=cl.exe
        - cd lexilla/scripts
        - CI=1 CXX=$CXX ./RunTest.bat
